name: testing1628

on:
  workflow_dispatch:

jobs:
  reproduce:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

     

      - name: Run setup-node against cyclic manifest
        uses: actions/setup-node@v7
        with:
          node-version-file: package.json
